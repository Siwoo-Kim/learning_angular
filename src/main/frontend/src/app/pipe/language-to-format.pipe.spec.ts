import { LanguageToFormatPipe } from './language-to-format.pipe';

describe('LanguageToFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageToFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
