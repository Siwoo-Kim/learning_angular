import { NewLineToBreakerPipe } from './new-line-to-breaker.pipe';

describe('NewLineToBreakerPipe', () => {
  it('create an instance', () => {
    const pipe = new NewLineToBreakerPipe();
    expect(pipe).toBeTruthy();
  });
});
